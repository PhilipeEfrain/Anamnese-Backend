# Variáveis de ambiente para testes

# MongoDB de Testes (NUNCA use o banco de produção!)
MONGO_URI_TEST=mongodb://localhost:27017/anamnese_test

# JWT Secret para testes
JWT_SECRET=test_secret_key_for_testing_only

# Porta do servidor de testes
PORT=3001

# Ambiente
NODE_ENV=test

# CORS
CORS_ORIGIN=*
